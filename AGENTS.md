# 에이전트 행동 규칙

## 저장소 작업

- 작업 시작 전 루트 `CLAUDE.md`를 읽고 저장소 구조와 작업·검증 규칙을 따른다.
- 세부 편집 방법은 작업 대상 디렉터리의 `README.md`를 참고한다.
- UI를 변경하는 PR 설명에는 데스크톱·모바일 화면 캡처와 확인한 동작을 포함한다. 시각적 오류 수정은 가능하면 수정 전·후를 함께 첨부한다.
- 캡처는 GitHub 첨부 이미지로 올린다. `gh pr create/edit --attach`를 지원하는 CLI를 사용하며 캡처 파일을 사이트 소스에 커밋하지 않는다.
