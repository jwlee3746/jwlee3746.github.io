# 에이전트 행동 규칙

## 저장소 작업

- 작업 시작 전 루트 `CLAUDE.md`를 읽고 저장소 구조와 작업·검증 규칙을 따른다.
- 세부 편집 방법은 작업 대상 디렉터리의 `README.md`를 참고한다.

## UI 변경 PR의 화면 캡처 (필수)

- UI를 변경하는 PR은 [PR #40](https://github.com/jwlee3746/jwlee3746.github.io/pull/40)처럼 **PR 설명 본문에 실제 화면 캡처를 반드시 첨부**한다. 텍스트로 검증 결과만 적거나 이미지 링크만 나열하는 것으로 대신하지 않는다.
- 본문에 **수정 전 → 수정 후 데스크톱 → 수정 후 모바일** 순서로 이미지를 배치한다. 기본 화면 너비는 데스크톱 1440px, 모바일 390px이며, 문제가 발생한 화면 크기가 다르면 해당 크기도 확인한다.
- 수정 전 화면은 변경 전에 확보한다. 새 화면 추가 등으로 비교할 화면이 없으면 그 이유를 명시하고 수정 후 데스크톱·모바일 캡처를 첨부한다.
- 각 이미지에 페이지 경로, 화면 크기, 확인한 상태·동작을 짧게 설명한다. 캐시·메뉴 열림 등 특정 조건에서 재현한 화면은 그 조건을 명시한다.
- 수정 후 캡처는 최종 코드로 브라우저에서 직접 확인한 화면을 사용한다. 캡처 후 UI를 다시 변경했다면 이미지를 갱신한다.
- `gh pr create/edit --attach`를 지원하는 GitHub CLI로 업로드하고, `![설명](GitHub 첨부 URL)` 형식으로 본문에 표시한다. PR 생성·수정 후 실제 첨부 URL과 이미지 표시를 확인한다.
- 캡처 파일은 임시 디렉터리에 보관하며 사이트 소스에 커밋하지 않는다. UI에 영향이 없는 문서·내부 코드 변경만 있는 PR은 캡처 대상에서 제외한다.

## 원본 프로젝트 식별자 비노출 (필수)

- 비공개·업무 프로젝트를 바탕으로 작성하는 글과 초안은 **역할·동작·구조만 일반화하여 설명**한다. 원본의 파일명·경로·저장소명·컴포넌트명·클래스명·함수명·변수명·설정 키·내부 이벤트명·커밋 식별자를 그대로 쓰지 않는다.
- 본문, 제목, 메타데이터, 코드 예시, 표, 다이어그램, SVG 속성·주석·접근성 설명, 이미지, 캡션, 링크, PR 설명과 첨부 화면에 동일하게 적용한다. 인용·구현 근거·예시라는 이유로 예외를 두지 않는다.
- Deep Agents·LangChain·LangGraph 같은 공개 라이브러리와 공식 문서로 확인되는 API는 사용할 수 있다. 프로젝트가 자체 정의한 이름은 역할을 나타내는 일반 명사로 바꾼다. 출처가 불분명한 식별자는 제거한다.
- 원본 코드 복사보다 자연어 또는 독립적으로 작성한 의사코드를 사용한다. 원본 맥락·그림 보존 규칙과 충돌하면 **식별자 비노출을 우선**한다. 내부 이름이 보이는 이전 화면도 다시 첨부하지 않는다.
- 편집 후 모든 변경 파일과 PR 본문·첨부 화면을 직접 검토하고 `npm run check:content`를 통과시킨다. 자동 검사는 등록된 식별자의 재등장을 막는 보조 장치이며, 새 식별자·이미지 속 글자·의미적 유사성은 직접 확인해야 한다.
- 알려진 식별자의 지문은 `scripts/site/private-identifier-fingerprints.json`에 추가한다. 원문 이름이나 대응표를 검증 코드·테스트·로그에 기록하지 않는다. 검사 통과를 위해 지문을 지우거나 공개 콘텐츠를 검사 대상에서 제외하지 않는다.
